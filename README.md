# Ag Atlas Agent

An agent for exploring the Africa Agriculture Adaptation Atlas.

```
pip install poetry
poetry install
poetry shell
```

then:

```
export OPENAI_API_KEY=your key here
python agent.py
```